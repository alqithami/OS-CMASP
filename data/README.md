# Data policy

This repository includes templates only. Do not commit raw maritime twin replay exports unless the data owner explicitly approves release.

Use `data/templates/berth1_replay_template.csv` as the export schema for Berth-1-Conflict replay.
